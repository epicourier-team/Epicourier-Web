---
description: Start the development environment including backend (FastAPI) and frontend (Next.js)
---
1. In the `backend` directory, run the command to start the server: `make dev`
// turbo
2. In the `web` directory, run the command to start the frontend: `npm run dev`
